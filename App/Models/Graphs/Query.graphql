type Query {
  player(dbname: String!, options: String): Player
  players(condition: String, options: String): [Player]

  game(dbname: String!, options: String): Game
  games(condition: String, options: String): [Game]

  cdkey(parameters: CDKeyQueryParameters!): CDKey
  cdkeys(condition: String, options: String): [CDKey]
}

