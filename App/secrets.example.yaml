---

  # Copy this example and create your own `secrets.yaml`
  mongo:
    user: root
    password: passwordOfMongo
    host: localhost:27017
    db:
      data: nameOfAppDatabase
      auth: nameOfAuthenticationDatabase